<h1>Listing decisions</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Order Number</th>
    <th>Short Name</th>
    <th>Short Description</th>
    <th>Description</th>
    <th>&nbsp;</th>
  </tr>

<% @decisions.each do |decision| %>
  <tr>
    <td><%=h decision.name %></td>
    <td><%=h decision.order_number %></td>
    <td><%=h truncate(decision.short_name,30) %></td>
    <td><%=h truncate(decision.short_desc,30) %></td>
    <td><%=h truncate(decision.description,30) %></td>
    <td><%= link_to 'Show', [:admin, decision] %>
    <%= link_to 'Edit', edit_admin_decision_path(decision) %>
    <%= link_to 'Destroy', [:admin,decision], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New decision', new_admin_decision_path, :class=> "button" %>
